#include <cs50.h>
#include <stdio.h>

int main(int n)
{
    int n;
    do
    {
        n = get_int("Height: ");
    }
    while (n > 0 && n < 9);


    for(int i, i < n, i--)
    {
        printf(" " * (num - 1) + "#" + "  " + "\n");
        num --;
    }
}


void fun(int n)
{

}
